<div class="container">
  <div class="row col-xs-12 col-md-3">

      <%= image_tag @activity.captain.picture(:thumb) %>
        <br>
      <%= @activity.captain.name %>

      <%= @activity.number_of_players %>
        <br>
        <br>
      <%= @activity.open %>
  </div>

  <div class="row col-sm-12 col-md-9">
    <div class="col-xs-12">
      <%= image_tag @activity.sport.picture(:large) %>
    </div>
    <div class="row col-xs-12">
      <div class="row col-sm-6">
        <div class="row col-sm-6">
          <ul>
            <li><%= @activity.name %></li>
            <li><%= @activity.sport.name %></li>
            <li><%= @activity.date %></li>
          </ul>
        </div>
        <div class="row col-sm-6">
          <h3>commentaires</h3>
        </div>
      </div>
      <div class="row col-sm-6 text-center">
        <% if @activity.location %>
          <%= image_tag "https://maps.googleapis.com/maps/api/staticmap?zoom=14&size=2000x200&sensor=false&maptype=roadmap&markers=color:red|#{@activity.location.full_address}", class: "img-responsive center-block" %>
        <% end %>
      </div>

      <% if @activity.captain == current_user %>
      <%= link_to "supprimer l'activité", group_activity_path(@group, @activity), method: :delete, class: "btn btn-primary" %>
      <% end %>

    </div>
  </div>

</div>


